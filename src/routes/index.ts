import { Login } from "src/pages";

const routes = [
  {
    path: "/login",
    name: "Login",
    exact: false,
    component: Login,
    icon: null,
    isSidebar: true,
  },
];

export default routes;
